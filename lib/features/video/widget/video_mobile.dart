import 'package:flutter/material.dart';

class VideoMobile extends StatelessWidget {
  const VideoMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("mobile"),);
  }
}